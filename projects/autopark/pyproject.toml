[project]
name = "autopark"
version = "0.1.0"
description = "Morning broadcast preparation automation for Buykings."
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"
